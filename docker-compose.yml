services:
  espn-plus:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./config:/app/config
    environment:
      - NODE_ENV=production
    command: npm start
    restart: always
